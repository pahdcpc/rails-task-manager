<%= form_with model: @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_area :details %>
  <% if @task.persisted? %>
    <%= f.check_box :completed %> Completed?
  <% end %>
  <%= f.submit %>
<% end %>
